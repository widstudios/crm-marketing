<?php

declare(strict_types=1);

namespace {{ Namespace }};

use WidStudios\Foundation\Services\BaseService;

/**
 * {{-- Quale capacita' fornisce, in una riga. --}}
 *
 * Un servizio esiste quando una capacita' non appartiene a nessuna entita' e
 * non e' una singola operazione: il calcolo di un listino, l'integrazione con
 * un sistema esterno, la generazione di un documento.
 *
 * Il criterio per distinguerlo da un'Action: l'Action rappresenta un fatto che
 * accade una volta ("registra il movimento"), il servizio una capacita' che si
 * puo' invocare quante volte si vuole senza cambiare nulla ("calcola il
 * prezzo").
 *
 * Se questa classe cresce fino ad avere metodi che non condividono nulla, non
 * e' un servizio: e' una cartella. In quel caso si divide.
 */
final class {{ Class }} extends BaseService
{
    public function __construct(
        // private readonly …,
    ) {}

    // public function …(…): …
    // {
    // }
}
